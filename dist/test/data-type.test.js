"use strict";
describe('data-type', function () {
    it('should be', function () {
        const name = "Theo";
        var angka = 10000;
        let isVIP = true;
        console.log(name);
        console.log(angka);
        console.log(isVIP);
    });
});
